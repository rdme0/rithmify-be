spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
        show_sql: false
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: false

logging:
  level:
    org.hibernate.SQL: WARN
    backend: INFO
